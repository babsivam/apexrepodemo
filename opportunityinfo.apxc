public class DbPractise 
{
    
     public static void amountGreater(integer Amount)
    {
        list<Opportunity>OppAmount =[
                                     SELECT 
                                     ID,Name 
                                     FROM Opportunity where Amount >:Amount];
        for(Opportunity oppA : OppAmount)
        {
            system.debug(oppA);
            system.debug('output::'oppA);
        }
        
    }
